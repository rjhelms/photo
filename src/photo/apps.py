"""
App configuration for the photo application.
"""
from django.apps import AppConfig


class PhotoConfig(AppConfig):
    """
    Default config for the photo application.
    """
    name = 'photo'
